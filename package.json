{
  "name": "smartnotes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "prisma generate && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npx prisma generate --prefix backend",
    "start": "node backend/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
